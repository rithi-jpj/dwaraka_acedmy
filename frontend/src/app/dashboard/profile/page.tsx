'use client';
import { useEffect, useState } from 'react';
import { api } from '@/lib/api';
export default function ProfilePage() { const [student, setStudent] = useState<any>(null); useEffect(() => { api.get('/student-profile/me').then(r => setStudent(r.data)).catch(() => setStudent(false)); }, []); if (student === null) return <div className="text-slate-500">Loading profile…</div>; if (!student) return <div className="card">Your student profile is not available.</div>; return <div className="space-y-6"><div><h1 className="text-2xl font-bold">My profile</h1><p className="text-sm text-slate-500">Your academy record is read-only.</p></div><div className="grid grid-cols-1 md:grid-cols-2 gap-6"><section className="card"><h2 className="font-semibold mb-4">Personal details</h2><p>{student.first_name} {student.last_name}</p><p className="text-sm text-slate-500 mt-1">{student.gender} · {student.dob}</p><p className="text-sm mt-3">Status: <span className="font-medium">{student.status}</span></p></section><section className="card"><h2 className="font-semibold mb-4">Academic details</h2><p>Class: {student.class_id}</p><p className="text-sm text-slate-500 mt-1">Batch: {student.batch?.name || 'Not assigned'}</p><p className="text-sm text-slate-500">Roll number: {student.roll_number || '—'}</p></section><section className="card"><h2 className="font-semibold mb-4">Contact and parent details</h2><p>{student.email}</p><p>{student.phone}</p><p className="text-sm text-slate-500">Parent: {student.parent_phone || '—'}</p></section><section className="card"><h2 className="font-semibold mb-4">Address</h2><p className="text-sm">{[student.address, student.city, student.state, student.country, student.pincode].filter(Boolean).join(', ') || '—'}</p></section></div></div>; }
